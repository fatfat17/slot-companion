import test from "node:test";import assert from "node:assert/strict";import { GUIDE_FEEDBACK_KEY,loadGuideFeedback,saveGuideFeedback } from "../src/lib/guideFeedback.ts";
test("guide feedback is browser-local and isolated by catalog",()=>{const memory=new Map<string,string>();Object.defineProperty(globalThis,"window",{configurable:true,value:{localStorage:{getItem:(key:string)=>memory.get(key)??null,setItem:(key:string,value:string)=>memory.set(key,value)}}});assert.equal(saveGuideFeedback({catalogId:"a",kind:"duplicate",sectionKey:null,note:"  重複內容  "},"2026-08-29T00:00:00Z")?.note,"重複內容");saveGuideFeedback({catalogId:"b",kind:"missing",sectionKey:"cz",note:"缺少 CZ"});assert.equal(loadGuideFeedback("a").length,1);assert.equal(loadGuideFeedback("b").length,1);assert.ok(memory.has(GUIDE_FEEDBACK_KEY));memory.delete(GUIDE_FEEDBACK_KEY);Reflect.deleteProperty(globalThis,"window")});
test("empty guide feedback is rejected",()=>{const memory=new Map<string,string>();Object.defineProperty(globalThis,"window",{configurable:true,value:{localStorage:{getItem:(key:string)=>memory.get(key)??null,setItem:(key:string,value:string)=>memory.set(key,value)}}});assert.equal(saveGuideFeedback({catalogId:"a",kind:"unclear",sectionKey:null,note:"   "}),null);Reflect.deleteProperty(globalThis,"window")});
