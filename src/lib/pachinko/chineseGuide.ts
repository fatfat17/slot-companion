import type {MachineGuideSectionKey} from "../../types/machineGuide.ts";
import type {PachinkoFullGuide,PachinkoPlayerGuideZh} from "../../types/pachinko.ts";

const titles:Partial<Record<MachineGuideSectionKey,string>>={features:"這台的基本規格",flow:"遊戲流程",bonus:"大當分配",at_art:"RUSH／ST／LT",play:"基本打法",special_events:"演出參考",payout:"交換率與回轉參考"};
export function pachinkoChineseInput(guide:PachinkoFullGuide){return{machine:{officialNameJa:guide.officialNameJa,manufacturer:guide.manufacturer},sections:guide.sections.map(section=>({key:section.key,titleJa:section.titleJa,paragraphsJa:section.paragraphsJa.slice(0,6).map(value=>value.slice(0,700)),tables:section.tables.slice(0,4).map(table=>({title:table.title,headers:table.headers,rows:table.rows.slice(0,12)}))}))}}
export function buildRuleBasedPachinkoGuide(guide:PachinkoFullGuide):PachinkoPlayerGuideZh{return{generator:"rules",overview:"已整理此機台的公開規格與流程。請依機台畫面的左打／右打指示操作，機率與出玉僅為規格參考。",goals:["先確認通常時的大當機率與 RUSH 突入方式","大當後依畫面判斷是否進入 RUSH／ST／LT","右打狀態結束後回到通常遊技"],sections:guide.sections.map(section=>({key:section.key,title:titles[section.key]??section.titleZh,summary:section.tables.length?"本區保留公開規格表，詳細數值可在下方查閱。":"本區已取得公開說明，完整日文保留於查證區。",points:[]})),generatedAt:guide.retrievedAt}}
function numbers(value:string){return[...value.matchAll(/\d+(?:[.,]\d+)?/g)].map(match=>match[0].replace(",","."))}
export function validatePachinkoChineseGuide(candidate:PachinkoPlayerGuideZh,guide:PachinkoFullGuide){const keys=new Set(guide.sections.map(section=>section.key)),sourceNumbers=new Set(numbers(JSON.stringify(pachinkoChineseInput(guide)))),text=JSON.stringify({overview:candidate.overview,goals:candidate.goals,sections:candidate.sections});return candidate.generator==="openai"&&Boolean(candidate.overview.trim())&&candidate.goals.length<=4&&candidate.sections.every(section=>keys.has(section.key)&&section.points.length<=4)&&numbers(text).every(value=>sourceNumbers.has(value))}
