export type SessionCompanionContext={machineName:string;currentState:string;observedGame:number;investmentYen:number;medals:number;records:Array<{label:string;value:string}>;guide:{corePlay:string|null;keyThings:string[];events:Array<{name:string;whatToSee:string;countingRule:string}>;sourceName:string;retrievedAt:string}|null};
export function sanitizeCompanionQuestion(value:unknown){return typeof value==="string"?value.trim().slice(0,300):""}
export function sanitizeCompanionContext(value:unknown):SessionCompanionContext|null{if(!value||typeof value!=="object")return null;const raw=value as Partial<SessionCompanionContext>;if(typeof raw.machineName!=="string"||!raw.machineName.trim())return null;const text=(input:unknown,max=180)=>typeof input==="string"?input.slice(0,max):"";return{machineName:text(raw.machineName,100),currentState:text(raw.currentState,60),observedGame:Number.isFinite(raw.observedGame)?Math.max(0,Number(raw.observedGame)):0,investmentYen:Number.isFinite(raw.investmentYen)?Math.max(0,Number(raw.investmentYen)):0,medals:Number.isFinite(raw.medals)?Math.max(0,Number(raw.medals)):0,records:Array.isArray(raw.records)?raw.records.slice(0,24).map(item=>({label:text(item?.label,80),value:text(item?.value,80)})).filter(item=>item.label):[],guide:raw.guide&&typeof raw.guide==="object"?{corePlay:text(raw.guide.corePlay,500)||null,keyThings:Array.isArray(raw.guide.keyThings)?raw.guide.keyThings.slice(0,6).map(item=>text(item,180)).filter(Boolean):[],events:Array.isArray(raw.guide.events)?raw.guide.events.slice(0,16).map(item=>({name:text(item?.name,100),whatToSee:text(item?.whatToSee,220),countingRule:text(item?.countingRule,180)})).filter(item=>item.name):[],sourceName:text(raw.guide.sourceName,80),retrievedAt:text(raw.guide.retrievedAt,50)}:null}}
export function companionDeveloperPrompt(){return "你是 Slot Companion 的遊玩中中文助手。只能使用提供的 Session 與 Machine Guide 結構化資料回答。用繁體中文，先給一句直接答案，再用最多 3 個短點說明。事件正式名稱可保留日文並附中文解釋。資料沒有寫就明確說目前指南沒有這項資料；不可編造機率、天井、Zone、設定差、操作結果或獲利建議。不可宣稱能準確判定設定或保證獲利。不要要求使用者相信 AI。不要使用 Markdown 星號、標題符號或表格。"}
