import { parseStructuredProfileHtml,type ProfileHeadingMapping } from "./html-structure.ts";
import type { StructuredExtractedSection } from "./types.ts";
export const nanaPressMappings:ProfileHeadingMapping[]=[[/AT初当たり確率/,"atInitialRate"],[/CZ（.*）確率/,"czInitialRate"],[/100G以内のCZ当選率/,"within100GameHitRate"],[/リプレイのAT直撃確率/,"atDirectHitRate"],[/AT引き戻し確率/,"atReturnRate"],[/CZ終了画面/,"czEndScreenIndications"],[/AT終了画面/,"endScreenIndications"],[/下段リプレイ確率/,"lowerReplayRate"],[/弱チェリーのCZ当選率/,"weakCherryCzSuccessRate"]];
function rebuild(section:StructuredExtractedSection,headers:string[],rows:string[][]){return{...section,tableHeaders:headers,rows,extractedValue:[headers,...rows].map(row=>row.join(" | "))}}
export function parseNanaPressProfileHtml(html:string){return parseStructuredProfileHtml(html,nanaPressMappings).map(section=>{if(section.metricKey!=="czInitialRate")return section;const marker=section.rows.findIndex(row=>row[0]==="設定"&&row.some(cell=>cell.includes("CZ合算確率")));if(marker<0)return section;const rows=section.rows.slice(marker+1).filter(row=>/^[1-6]$/.test(row[0]??"")).map(row=>[row[0],row[1]]);return rows.length?rebuild(section,["設定","CZ合算確率"],rows):section})}
