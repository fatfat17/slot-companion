"use client";
import { useState } from "react";
import type { SessionCompanionContext } from "@/lib/ai/companion";
type Message={role:"user"|"assistant";text:string};
const suggestions=["我現在最該注意什麼？","這台的 CZ、AT 怎麼辨認？","哪些按鈕需要記錄？"];
export function AICompanionDrawer({context,onClose}:{context:SessionCompanionContext;onClose:()=>void}){const [question,setQuestion]=useState(""),[messages,setMessages]=useState<Message[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState("");async function send(next=question){const clean=next.trim();if(!clean||loading)return;setMessages(items=>[...items,{role:"user",text:clean}]);setQuestion("");setError("");setLoading(true);try{const response=await fetch("/api/ai/session-companion",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:clean,context})}),payload=await response.json();if(!response.ok)throw new Error(payload.error?.message??"AI 暫時無法回答");setMessages(items=>[...items,{role:"assistant",text:payload.answer}])}catch(value){setError(value instanceof Error?value.message:"AI 暫時無法回答")}finally{setLoading(false)}}return <div className="drawer-backdrop" onClick={onClose}><aside className="drawer ai-companion-drawer" onClick={event=>event.stopPropagation()}><div className="drawer-handle"/><div className="drawer-head"><div><span>AI COMPANION</span><h2>陪你看懂現在這局</h2></div><button onClick={onClose}>×</button></div><div className="ai-summary"><span>{context.machineName}</span><b>{context.observedGame.toLocaleString()} G</b><span>狀態 {context.currentState}</span><span>投入 ¥{context.investmentYen.toLocaleString()} · 持枚 {context.medals}</span></div><div className="ai-chat">{messages.length===0&&<div className="mock-reply">我只會依這台已整理的機台指南與目前 Session 紀錄回答。可以直接問現在該注意什麼。</div>}{messages.map((message,index)=><article className={message.role} key={`${message.role}-${index}`}>{message.text}</article>)}{loading&&<article className="assistant">整理指南與目前紀錄中…</article>}{error&&<p className="ai-error">{error}</p>}</div><div className="suggestions">{suggestions.map(item=><button key={item} onClick={()=>void send(item)}>{item}</button>)}</div><form className="ai-input" onSubmit={event=>{event.preventDefault();void send()}}><input maxLength={300} value={question} onChange={event=>setQuestion(event.target.value)} placeholder="輸入想問的問題…"/><button disabled={loading||!question.trim()}>送出</button></form></aside></div>}
