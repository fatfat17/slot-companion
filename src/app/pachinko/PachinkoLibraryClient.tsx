"use client";
import Link from "next/link";
import {useMemo,useState} from "react";
import type {PachinkoCatalogRecord} from "@/types/pachinko";
import {searchPachinkoCatalog} from "@/lib/pachinko/catalog";
import {PachinkoCoverImage} from "@/components/PachinkoCoverImage";
const typeLabels:Record<PachinkoCatalogRecord["machineType"],string>={smart_pachinko:"智慧柏青哥",digital:"數位柏青哥",hanemono:"羽根物",other:"其他"};
export function PachinkoLibraryClient({records}:{records:PachinkoCatalogRecord[]}){const[query,setQuery]=useState(""),[machineType,setMachineType]=useState("");const result=useMemo(()=>searchPachinkoCatalog(records,query).filter(item=>!machineType||item.machineType===machineType),[records,query,machineType]);return<main className="page catalog-library pachinko-library"><section className="catalog-management card pachinko-management"><div><span>PACHINKO LIBRARY</span><strong>{records.length} 台柏青哥試行資料</strong><small>資料庫、機台說明與遊玩紀錄均與柏青嫂分開。</small></div><Link className="secondary-button" href="/catalog">切換柏青嫂</Link></section><section className="catalog-search-panel"><div className="catalog-search-row"><input className="input" aria-label="搜尋柏青哥機種" placeholder="搜尋柏青哥名稱、中文名或メーカー" value={query} onChange={event=>setQuery(event.target.value)}/></div><select className="select mt-3" aria-label="柏青哥類型" value={machineType} onChange={event=>setMachineType(event.target.value)}><option value="">所有類型</option>{Object.entries(typeLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></section><div className="section-title catalog-result-title"><h2>柏青哥機種</h2><span>{result.length} 台</span></div>{result.length?<div className="catalog-card-grid">{result.map(record=><article className="catalog-visual-card pachinko-card" key={record.id}><Link href={`/pachinko/${record.id}`}><PachinkoCoverImage record={record}/><div className="catalog-card-body"><h2>{record.displayNameZh||record.officialNameJa}</h2>{record.displayNameZh&&<p>{record.officialNameJa}</p>}<span>{record.manufacturer}</span><div className="catalog-estimator-badge record-only">{typeLabels[record.machineType]}</div><div className="catalog-card-status">機台說明 · 簡易紀錄</div></div></Link></article>)}</div>:<div className="catalog-empty card"><strong>找不到符合條件的柏青哥</strong><p>請更換名稱或清除類型篩選。</p></div>}<div className="notice mt-4">首批資料取自 P-WORLD 2026 年 9 月公開新台清單；機率與出玉只作規格說明，不預測中獎。</div></main>}
