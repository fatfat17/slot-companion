import {pachinkoCatalogRepository} from "@/lib/pachinko/repository.server";
import {canonicalPWorldImageUrl} from "@/lib/machine-guide/visualGuide";
import {CATALOG_COVER_MAX_BYTES} from "@/lib/catalog/cover";
export const dynamic="force-dynamic";
export async function GET(request:Request,{params}:{params:Promise<{catalogId:string}>}){const{catalogId}=await params,source=new URL(request.url).searchParams.get("source"),record=(await pachinkoCatalogRepository.list()).find(item=>item.id===catalogId);if(!record||!source)return new Response("Not found",{status:404});const canonical=canonicalPWorldImageUrl(source,record.sourceUrl),allowed=record.sourceImageUrl?canonicalPWorldImageUrl(record.sourceImageUrl,record.sourceUrl):null;if(!canonical||canonical!==allowed)return new Response("Invalid image source",{status:400});const upstream=await fetch(canonical,{headers:{Accept:"image/avif,image/webp,image/png,image/jpeg","User-Agent":"Slot Companion pachinko cover","Referer":record.sourceUrl},cache:"no-store",signal:AbortSignal.timeout(12000)});if(!upstream.ok)return new Response("Image unavailable",{status:upstream.status});const contentType=(upstream.headers.get("content-type")??"").split(";")[0].toLowerCase();if(!["image/jpeg","image/png","image/webp"].includes(contentType))return new Response("Invalid image",{status:415});const bytes=await upstream.arrayBuffer();if(!bytes.byteLength||bytes.byteLength>CATALOG_COVER_MAX_BYTES)return new Response("Image too large",{status:413});return new Response(bytes,{headers:{"Content-Type":contentType,"Cache-Control":"public, max-age=86400, stale-while-revalidate=604800","X-Content-Type-Options":"nosniff"}})}
