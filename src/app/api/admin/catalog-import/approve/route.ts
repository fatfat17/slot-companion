import { catalogRepository } from "@/lib/catalog/repository.server";
import { ApprovalValidationError,validateApprovalDecisions } from "@/lib/catalog/approval";
import type { CatalogImportDecision } from "@/types/catalog";
import { authorizeCatalogAdmin } from "@/lib/catalog/adminAuth.server";

export const runtime="nodejs";
export async function POST(request:Request){if(!authorizeCatalogAdmin(request))return Response.json({error:{message:"管理密碼不正確，無法更新機種資料庫。"}},{status:401});let received=0;try{const {decisions}=await request.json() as {decisions?:CatalogImportDecision[]};received=Array.isArray(decisions)?decisions.length:0;validateApprovalDecisions(decisions);const result=await catalogRepository.approve(decisions);const months=decisions.map(item=>item.candidate.introducedAt?.slice(0,7)).filter((value):value is string=>Boolean(value)).sort();let auditLogged=true;try{await catalogRepository.recordImportJob({sourceName:decisions[0]?.candidate.sourceName??"P-WORLD",rangeStart:months[0]??null,rangeEnd:months.at(-1)??null,status:"approved",received,processed:result.processed,imported:result.imported,merged:result.merged,skipped:result.skipped})}catch{auditLogged=false}return Response.json({received,processed:result.processed,imported:result.imported,merged:result.merged,skipped:result.skipped,total:result.total,auditLogged})}catch(error){if(error instanceof ApprovalValidationError)return Response.json({error:{message:error.message},received,processed:0,imported:0,merged:0,skipped:0},{status:422});return Response.json({error:{message:"Catalog 寫入失敗。"},received,processed:0,imported:0,merged:0,skipped:0},{status:500})}}
