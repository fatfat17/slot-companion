import { AI_CONFIG } from "@/lib/ai/config";
import { identifyMachineOnServer } from "@/lib/ai/service.server";
import { AIProviderError } from "@/lib/ai/types";

export const runtime="nodejs";
export async function POST(request:Request){try{let form:FormData;try{form=await request.formData()}catch{return Response.json({error:{code:"missing_image",message:"請先選擇一張圖片。"}},{status:400})}const image=form.get("image");if(!(image instanceof File))return Response.json({error:{code:"missing_image",message:"請先選擇一張圖片。"}},{status:400});if(!image.type.startsWith("image/"))return Response.json({error:{code:"invalid_image",message:"只能使用圖片檔案。"}},{status:415});if(image.size>AI_CONFIG.maxImageBytes)return Response.json({error:{code:"image_too_large",message:"圖片請勿超過 8MB。"}},{status:413});const bytes=Buffer.from(await image.arrayBuffer());const dataUrl=`data:${image.type};base64,${bytes.toString("base64")}`;return Response.json(await identifyMachineOnServer({dataUrl,fileName:image.name,mimeType:image.type}))}catch(error){if(error instanceof AIProviderError)return Response.json({error:{code:error.code,message:error.message}},{status:error.status});if(error instanceof DOMException&&error.name==="AbortError")return Response.json({error:{code:"cancelled",message:"已取消辨識。"}},{status:499});return Response.json({error:{code:"request_failed",message:"辨識服務發生錯誤，請稍後再試。"}},{status:500})}}
